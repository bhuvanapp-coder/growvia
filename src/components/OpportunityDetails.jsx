import { ArrowUpRight, CalendarDays, Check, MapPin, Target, Users, X } from 'lucide-react'
import { categoryMeta } from '../data/opportunities'

export function OpportunityDetails({ opportunity, onClose, reminder, onToggleReminder, saved, onToggleSaved, onPrepare }) {
  const meta = categoryMeta[opportunity.category]
  const Icon = meta.icon
  return <div className="modal-backdrop" onMouseDown={onClose}><div className="detail-modal" onMouseDown={(event) => event.stopPropagation()}><button className="close-button" onClick={onClose}><X size={18} /></button><div className={`detail-hero ${opportunity.color}`}><span className={`category-pill ${meta.className}`}><Icon size={14} />{meta.label}</span><div className="org-name">{opportunity.org}</div><h2>{opportunity.title}</h2><div className="detail-facts"><span><CalendarDays size={15} />{opportunity.date}</span><span><MapPin size={15} />{opportunity.location}</span><span><Users size={15} />Teams of 2–4</span></div></div><div className="detail-body"><div className="why-box"><Target size={18} /><div><strong>Why am I seeing this?</strong><p>{opportunity.reason}</p></div></div><div className="detail-columns"><div><h4>You bring</h4><div className="skill-list">{opportunity.skills.map((skill) => <span className="skill good" key={skill}><Check size={13} />{skill}</span>)}</div></div><div><h4>Good next stretch</h4><div className="skill-list">{opportunity.missing.map((skill) => <span className="skill" key={skill}><Target size={13} />{skill}</span>)}</div></div></div><div className="detail-actions"><button className="button dark-button" onClick={opportunity.category === 'stretch' ? onPrepare : onToggleReminder}>{opportunity.category === 'stretch' ? 'Prepare me' : reminder ? 'Reminder set' : 'Remind me'} <ArrowUpRight size={15} /></button><button className={saved ? 'button secondary saved' : 'button secondary'} onClick={onToggleSaved}><Check size={15} />{saved ? 'Saved' : 'Save opportunity'}</button></div></div></div></div>
}