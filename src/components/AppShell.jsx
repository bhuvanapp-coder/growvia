import { Bell, ChevronRight, Compass, Heart, HelpCircle, Home, LayoutDashboard, LogOut, Menu, UserRound } from 'lucide-react'

export function AppShell({ activePage, studentName = 'Ananya Shah', onNavigate, savedCount, onLogout, children }) {
  const navItems = [[Home, 'Home'], [LayoutDashboard, 'Dashboard'], [Heart, 'Dreams'], [UserRound, 'Profile']]
  const initials = studentName.split(' ').map((part) => part[0]).join('').slice(0, 2).toUpperCase()
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><span /></span><span>grow<span>via</span></span></div><button className="profile-chip" onClick={() => onNavigate('Profile')}><div className="avatar">{initials}</div><div><strong>{studentName}</strong><span>Student profile</span></div><ChevronRight size={15} /></button><nav className="main-nav">{navItems.map(([Icon, label]) => <button key={label} className={activePage === label ? 'nav-item active' : 'nav-item'} onClick={() => onNavigate(label)}><Icon size={18} />{label}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item"><HelpCircle size={18} />Help center</button><button className="nav-item" onClick={onLogout}><LogOut size={18} />Log out</button><div className="upgrade-card"><strong>Shape your Opportunity DNA</strong><p>Tell us where you are going next.</p><button onClick={() => onNavigate('Onboarding')}>Complete DNA <ChevronRight size={14} /></button></div></div></aside><main className="main-content"><header className="topbar"><button className="mobile-menu"><Menu size={20} /></button><div className="breadcrumb"><span>{activePage}</span><ChevronRight size={14} /><strong>2026</strong></div><div className="top-actions"><button className="icon-button notification"><Bell size={19} /><i /></button><div className="avatar small">{initials}</div></div></header>{children}</main></div>
}

function CalendarIcon(props) { return <span {...props}>◷</span> }