import { Bell, Bookmark, ChevronRight, Compass, HelpCircle, LayoutDashboard, Menu, Upload } from 'lucide-react'

export function AppShell({ activePage, onNavigate, savedCount, children }) {
  const navItems = [[LayoutDashboard, 'Dashboard'], [Compass, 'Explore'], [Bookmark, 'Saved'], [CalendarIcon, 'Calendar']]
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><span /></span><span>grow<span>via</span></span></div><div className="profile-chip"><div className="avatar">AS</div><div><strong>Ananya Shah</strong><span>Student profile</span></div><ChevronRight size={15} /></div><nav className="main-nav">{navItems.map(([Icon, label]) => <button key={label} className={activePage === label ? 'nav-item active' : 'nav-item'} onClick={() => onNavigate(label)}><Icon size={18} />{label}{label === 'Saved' && savedCount ? <em>{savedCount}</em> : null}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item" onClick={() => onNavigate('Organizer')}><Upload size={18} />Organizer tools</button><button className="nav-item"><HelpCircle size={18} />Help center</button><div className="upgrade-card"><strong>Shape your Opportunity DNA</strong><p>Tell us where you are going next.</p><button onClick={() => onNavigate('Onboarding')}>Complete DNA <ChevronRight size={14} /></button></div></div></aside><main className="main-content"><header className="topbar"><button className="mobile-menu"><Menu size={20} /></button><div className="breadcrumb"><span>{activePage}</span><ChevronRight size={14} /><strong>2026</strong></div><div className="top-actions"><button className="icon-button notification"><Bell size={19} /><i /></button><div className="avatar small">AS</div></div></header>{children}</main></div>
}

function CalendarIcon(props) { return <span {...props}>◷</span> }