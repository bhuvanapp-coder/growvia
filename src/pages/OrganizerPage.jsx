import { ArrowUpRight, Check, FileText, Upload } from 'lucide-react'
import { useState } from 'react'

export function OrganizerPage({ onBack }) { const [uploaded, setUploaded] = useState(false); return <div className="page-wrap organizer-page"><section className="welcome-row"><div><div className="eyebrow">ORGANIZER STUDIO</div><h1>Publish something<br />worth finding.</h1><p className="lead">Upload a poster. We’ll turn it into a structured opportunity for the right students.</p></div><button className="button secondary" onClick={onBack}>Back to student view</button></section><div className="organizer-grid"><section className="organizer-panel"><div className="panel-heading"><div><h2>{uploaded ? 'Review extracted details' : 'Create an opportunity'}</h2><p>AI extraction is represented here with a reviewable demo flow.</p></div><span className="status-badge">{uploaded ? 'Ready to review' : 'Step 1 of 2'}</span></div>{!uploaded ? <button className="upload-zone" onClick={() => setUploaded(true)}><Upload size={25} /><strong>Upload event poster</strong><span>PNG, JPG or PDF · up to 10 MB</span></button> : <div className="extracted"><div className="poster-thumb"><FileText size={26} /><span>poster-ai-summit.png</span><Check size={17} /></div><label>Event name<input defaultValue="Women in Tech AI Summit" /></label><label>Registration deadline<input defaultValue="October 28, 2026" /></label><label>Location<input defaultValue="Mumbai · In person" /></label><button className="button dark-button publish" onClick={onBack}>Review & publish <ArrowUpRight size={15} /></button></div>}</section><aside className="organizer-aside"><div className="eyebrow">PUBLISHING CHECKLIST</div>{['Upload the event poster', 'Review extracted information', 'Publish to student radar'].map((item, index) => <div className={uploaded || index === 0 ? 'check-row done' : 'check-row'} key={item}><span>{uploaded || index === 0 ? <Check size={13} /> : index + 1}</span>{item}</div>)}</aside></div></div> }