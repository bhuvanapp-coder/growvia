import { Check, ChevronRight, Target } from 'lucide-react'
import { useState } from 'react'

const steps = [{ title: 'What can you do today?', sub: 'Pick everything that feels familiar.', options: ['Python', 'JavaScript', 'React', 'C / C++', 'UI / UX', 'Public speaking', 'Data analysis', 'Git & GitHub'] }, { title: 'What pulls you in?', sub: 'We’ll use this to find your kind of room.', options: ['AI / ML', 'Web development', 'Entrepreneurship', 'Cybersecurity', 'Research', 'Design', 'Electronics', 'Community'] }, { title: 'Who are you becoming?', sub: 'Choose the growth goals that matter this semester.', options: ['Technical skills', 'Communication', 'Leadership', 'Teamwork', 'Problem solving', 'Creativity', 'Entrepreneurship', 'Confidence'] }]

export function OnboardingPage({ onComplete }) { const [step, setStep] = useState(0); const [selected, setSelected] = useState([]); const current = steps[step]; const toggle = (option) => setSelected((items) => items.includes(option) ? items.filter((item) => item !== option) : [...items, option]); return <div className="onboarding-page"><div className="onboarding-top"><div className="brand"><span className="brand-mark"><span /></span><span>grow<span>via</span></span></div><span>YOUR OPPORTUNITY DNA <b>{step + 1} / 3</b></span></div><div className="onboarding-card"><div className="dna-orb"><Target size={30} /></div><h1>{current.title}</h1><p>{current.sub}</p><div className="choice-grid">{current.options.map((option) => <button className={selected.includes(option) ? 'choice selected' : 'choice'} onClick={() => toggle(option)} key={option}>{selected.includes(option) && <Check size={15} />}{option}</button>)}</div><div className="onboarding-footer"><span>{selected.length} selected</span><button className="button dark-button" onClick={() => step < 2 ? setStep(step + 1) : onComplete()}>{step < 2 ? 'Continue' : 'Open my dashboard'} <ChevronRight size={16} /></button></div></div></div> }